{% extends 'base.html.twig' %}


{% block body %}

{% block title %}Index Categorie{% endblock %}
    <h1>Categorie index</h1>
<table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>resumé</th>
                <th>Action</th>

            </tr>
        </thead>
        <tbody>
        {% for categories in categorie %}
        {# https://twig.symfony.com/doc/3.x/tags/for.html #}
            <tr>
                {#<td>{{ categorie.id }}</td>#}
                <td>{{ categorie.Titre }}</td>
                <td>{{ categorie.Resume }}</td>
                <td> <a href="{{ path('affichage', {'id':categorie.id}) }}"> Afficher </a> </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}

